<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>

<%= attachment_image_tag @user, :image, :fill, 60, 60, fallback: "no_image.jpg" %>
<%= @user.name %>
フォロワー数
<%=link_to followers_user_path(@user) do %>
              <%= @user.followers.count %>
            <% end %>
フォロー数
<%=link_to followings_user_path(@user) do %>
              <%= @user.followings.count %>
            <% end %>
<% if user_signed_in? && @user != current_user%>
                  <div id=<%= "follow_form" +  @user.id.to_s %>>
                  <% if current_user.followings?(@user) %>
                    <%= render "relationships/unfollow", user: @user %>
                  <% else %>
                    <%= render "relationships/follow", user: @user %>
                  <% end %>
                  </div>
                <% end %>

<!--ログインしているユーザーのみ編集可能-->
<%= @user.user_genre %>
<br>
<% if @user == current_user %>
<%= link_to "Usersのedit", edit_user_path(@user) %>
<% end %>

<%= link_to "Usersのindex", users_path %>

<%= link_to "tweetのnew", new_tweet_path %>

<%= link_to "tweetのindex", tweets_path %>

